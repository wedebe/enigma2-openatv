<!--suppress XmlUnboundNsPrefix -->
<wizard>
	   <step id="start" nextstep="end">
				<text value="Optimise the position of on-screen graphics such as menus, EPG etc. by adjusting the horizontal and vertical alignment, width and height until all sides of the red box touch the edges of your screen.\nPress ≪OK≫ when you're ready to continue..." />
				<displaytext value="Configure interface" />
				<config screen="UserInterfacePositioner" module="UserInterfacePositioner" type="ConfigList" />
				<code>
self.clearSelectedKeys()
self.selectKey("OK")
self.selectKey("UP")
self.selectKey("DOWN")
self.selectKey("LEFT")
self.selectKey("RIGHT")
				</code>
				<code pos="after" async="yes">
self.markDone()
self.close()
				</code>
		</step>
</wizard>

